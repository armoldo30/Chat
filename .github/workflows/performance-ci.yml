name: Performance 0.16.1 Validation
on:
  push:
    branches: [performance-0.16.1]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Performance transform certification
        run: |
          cp src/main.js /tmp/main-performance-check.js
          node scripts/performance-patch-v2.mjs /tmp/main-performance-check.js
          node --check /tmp/main-performance-check.js
          grep -q "equipmentSideCache" /tmp/main-performance-check.js
          grep -q "techSideCache" /tmp/main-performance-check.js
          grep -q "divisionSideCache" /tmp/main-performance-check.js
          grep -q "refreshDivisionLab" /tmp/main-performance-check.js
      - name: Production regression suite
        run: npm test
      - name: Build optimized static site
        run: npm run build
      - name: Verify optimized bundle markers
        run: |
          grep -q "equipmentSideCache" dist/src/main.js
          grep -q "techSideCache" dist/src/main.js
          grep -q "divisionSideCache" dist/src/main.js
          grep -q "refreshDivisionLab" dist/src/main.js

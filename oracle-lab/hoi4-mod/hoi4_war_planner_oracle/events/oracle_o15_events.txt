add_namespace = oracle_o15
country_event = {
    id = oracle_o15.1
    hidden = yes
    is_triggered_only = yes
    immediate = {
        if = {
            limit = { has_country_flag = oracle_o15_running }
            add_to_variable = { oracle_o15_hour = 1 }
            oracle_o15_capture_sample = yes
            if = {
                limit = {
                    NOT = {
                        check_variable = {
                            var = oracle_o15_hour
                            value = 161
                            compare = less_than
                        }
                    }
                }
                clr_country_flag = oracle_o15_running
                if = {
                    limit = { has_dynamic_modifier = { modifier = oracle_o15_attack_target } }
                    remove_dynamic_modifier = { modifier = oracle_o15_attack_target }
                    force_update_dynamic_modifier = yes
                }
                POL = {
                    if = {
                        limit = { has_dynamic_modifier = { modifier = oracle_o15_defense_target } }
                        remove_dynamic_modifier = { modifier = oracle_o15_defense_target }
                        force_update_dynamic_modifier = yes
                    }
                }
                clr_country_flag = oracle_o15_prepared
                if = {
                    limit = {
                        OR = {
                            has_dynamic_modifier = { modifier = oracle_o15_attack_target }
                            POL = { has_dynamic_modifier = { modifier = oracle_o15_defense_target } }
                        }
                    }
                    log = "WPO15 END hour=[?oracle_o15_hour] reason=trial161-complete modifiersRemoved=no cleanupFailure=yes"
                }
                else = {
                    log = "WPO15 END hour=[?oracle_o15_hour] reason=trial161-complete modifiersRemoved=yes cleanupFailure=no"
                }
            }
        }
    }
}

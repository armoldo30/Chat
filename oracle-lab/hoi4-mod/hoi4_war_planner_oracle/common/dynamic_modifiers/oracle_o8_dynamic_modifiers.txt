# O8 defense-point integerization probe.
# GER attack reuses the accepted O7 integer-center target.
# POL defense is reduced from the controlled 255 baseline toward 10.

oracle_o8_attack_target = {
    enable = { always = yes }
    army_infantry_attack_factor = -0.721
}

oracle_o8_defense_target = {
    enable = { always = yes }
    # 255 * (1 - 0.9607843137) ~= 10.0 in the controlled baseline.
    # Executable acceptance still requires the live panel target.
    army_infantry_defence_factor = -0.9607843137
}

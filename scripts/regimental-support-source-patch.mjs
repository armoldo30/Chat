import { readFile, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';

const root=resolve(import.meta.dirname,'..');

async function patch(path,transforms){
  const file=resolve(root,path);let text=await readFile(file,'utf8');
  for(const [from,to,label] of transforms){
    if(!text.includes(from))throw new Error(`Regimental support patch marker missing (${label}) in ${path}`);
    text=text.replace(from,to);
  }
  await writeFile(file,text);
}

await patch('src/parser.js',[[`      id,group:last(raw.group),types:items(raw.type),categories:items(raw.categories),\n      width:num(last(raw.combat_width)),hp:num(last(raw.max_strength)),org:num(last(raw.max_organisation)),manpower:num(last(raw.manpower)),`,`      id,group:last(raw.group),types:items(raw.type),categories:items(raw.categories),\n      allowedBattalionGroups:items(raw.allowed_battalion_groups),\n      divisional:raw.divisional===undefined?undefined:last(raw.divisional),regimental:raw.regimental===undefined?undefined:last(raw.regimental),\n      sameSupportType:items(raw.same_support_type),battalionMult:raw.battalion_mult===undefined?[]:(Array.isArray(raw.battalion_mult)?raw.battalion_mult:[raw.battalion_mult]).map(plainMap),\n      width:num(last(raw.combat_width)),hp:num(last(raw.max_strength)),org:num(last(raw.max_organisation)),manpower:num(last(raw.manpower)),`,'sub-unit structural fields']]);

await patch('src/gameData.js',[
[`export function classifySubUnit(u){\n  const text=words(u),group=String(u?.group||'').toLowerCase(),categories=Array.isArray(u?.categories)?u.categories.map(x=>String(x).toLowerCase()):[];\n  const support=u?.width===undefined||u?.width===0||group==='support'||/category_(?:all_)?support|\\bsupport\\b/.test(text);\n  const hasExplicitCategories=categories.length>0;\n  const regimental=support&&(hasExplicitCategories?categories.includes('category_regimental_support_battalions'):/regiment|regimental|infantry_gun|heavy_weapon|rocket_battery|anti_air_battery|anti_tank_battery|tank_destroyer.*support|spaa.*support/.test(text));\n  let regimentGroup=null;\n  if(/armor|tank|spaa|tank_destroyer/.test(text))regimentGroup='armor';\n  else if(/motor|mechanized|mobile/.test(text))regimentGroup='mobile';\n  else if(regimental)regimentGroup='infantry';\n  return {support,regimental,regimentGroup};\n}`,`export function classifySubUnit(u){\n  const text=words(u),group=String(u?.group||'').toLowerCase(),categories=Array.isArray(u?.categories)?u.categories.map(x=>String(x).toLowerCase()):[];\n  const support=u?.width===undefined||u?.width===0||group==='support'||/category_(?:all_)?support|\\bsupport\\b/.test(text);\n  const hasExplicitCategories=categories.length>0,explicitRegimental=typeof u?.regimental==='boolean'?u.regimental:null;\n  const regimental=support&&(explicitRegimental!==null?explicitRegimental:(hasExplicitCategories?categories.includes('category_regimental_support_battalions'):/regiment|regimental|infantry_gun|heavy_weapon|rocket_battery|anti_air_battery|anti_tank_battery|tank_destroyer.*support|spaa.*support/.test(text)));\n  let regimentGroup=null;\n  if(/armor|tank|spaa|tank_destroyer/.test(text))regimentGroup='armor';\n  else if(/motor|mechanized|mobile/.test(text))regimentGroup='mobile';\n  else if(regimental)regimentGroup='infantry';\n  const allowedBattalionGroups=Array.isArray(u?.allowedBattalionGroups)?[...new Set(u.allowedBattalionGroups.map(String))]:[];\n  return {support,regimental,regimentGroup,allowedBattalionGroups,divisional:u?.divisional,sameSupportType:Array.isArray(u?.sameSupportType)?[...u.sameSupportType]:[],battalionMult:Array.isArray(u?.battalionMult)?clone(u.battalionMult):[]};\n}`,'source-aware support classification'],
[`const record={...(kind.support?supports[id]:battalions[id]||{}),...computed,id,name:(kind.support?supports[id]?.name:battalions[id]?.name)||humanize(raw.id),group:normalizedGroup(raw),gameId:raw.id,categories:[...(raw.categories||[])],types:[...(raw.types||[])],regimentalSupport:kind.regimental,regimentGroup:kind.regimentGroup,requirements:indexedRequirements(pack,'subUnits',raw.id),prerequisite:raw.prerequisite||null,terrain:{},terrainModifiers,terrainSource:Object.keys(terrainModifiers).length?'game-pack-source':'source-terrain-not-retained',terrainRuntimeClassification:Object.keys(terrainModifiers).length?'combat-formulas-audited':'no-source-terrain-block'};`,`const record={...(kind.support?supports[id]:battalions[id]||{}),...computed,id,name:(kind.support?supports[id]?.name:battalions[id]?.name)||humanize(raw.id),group:normalizedGroup(raw),gameId:raw.id,categories:[...(raw.categories||[])],types:[...(raw.types||[])],regimentalSupport:kind.regimental,regimentGroup:kind.regimentGroup,allowedBattalionGroups:[...(kind.allowedBattalionGroups||[])],divisional:kind.divisional,sameSupportType:[...(kind.sameSupportType||[])],battalionMult:clone(kind.battalionMult||[]),requirements:indexedRequirements(pack,'subUnits',raw.id),prerequisite:raw.prerequisite||null,terrain:{},terrainModifiers,terrainSource:Object.keys(terrainModifiers).length?'game-pack-source':'source-terrain-not-retained',terrainRuntimeClassification:Object.keys(terrainModifiers).length?'combat-formulas-audited':'no-source-terrain-block'};`,'hydrate support structure']
]);

await patch('src/main.js',[
[`import { DESIGNER_COLS, DESIGNER_ROWS, blankGrid, normalizeGrid, countsToGrid, gridToCounts, filledInRegiment, fillRegiment, regimentGroup as gridRegimentGroup, canPlaceBattalion } from './designer.js';`,`import { DESIGNER_COLS, DESIGNER_ROWS, blankGrid, normalizeGrid, countsToGrid, gridToCounts, filledInRegiment, fillRegiment, regimentGroup as gridRegimentGroup, canPlaceBattalion } from './designer.js';\nimport { applyRegimentalSupportCompatibilityFallback, regimentalSupportAllowed, supportAllowedBattalionGroups } from './regimental-support-1192.js';`,'regimental support import'],
[`const importedRegimentalSupports=state.dataPack?importedRegimentalSupportIds(supports):[];`,`applyRegimentalSupportCompatibilityFallback(supports);\nconst importedRegimentalSupports=state.dataPack?importedRegimentalSupportIds(supports):[];`,'bundled compatibility fallback'],
[`const LEGACY_REGIMENTAL_MAP={support_artillery:'regimental_infantry_guns',support_at:'regimental_at',support_aa:'regimental_aa'};`,`const LEGACY_REGIMENTAL_MAP={support_artillery:'field_guns',regimental_infantry_guns:'field_guns',support_at:'anti_tank_battery',regimental_at:'anti_tank_battery',support_aa:'anti_air_battery',regimental_aa:'anti_air_battery'};`,'legacy migration'],
[`const SUPPORT_CODES={engineer:'ENG',support_artillery:'ART',recon:'REC',support_at:'AT',support_aa:'AA',regimental_infantry_guns:'IG',regimental_at:'RAT',regimental_aa:'RAA',logistics:'LOG',signal:'SIG'};`,`const SUPPORT_CODES={engineer:'ENG',support_artillery:'ART',recon:'REC',support_at:'AT',support_aa:'AA',regimental_infantry_guns:'IG',regimental_at:'RAT',regimental_aa:'RAA',fire_support:'HWC',mot_fire_support:'MHW',field_guns:'IFG',rocket_battery:'RKT',anti_air_battery:'RAA',anti_tank_battery:'RAT',light_tank_destroyer_support:'LTD',medium_tank_destroyer_support:'MTD',heavy_tank_destroyer_support:'HTD',modern_tank_destroyer_support:'OTD',light_sp_anti_air_support:'LAA',medium_sp_anti_air_support:'MAA',heavy_sp_anti_air_support:'HAA',modern_sp_anti_air_support:'OAA',logistics:'LOG',signal:'SIG'};`,'regimental support visual codes'],
[`function regimentalBaselineCompatible(side,c,key=null){const group=regimentGroup(side,c);if(!key)return !!group;const required=supports[key]?.regimentGroup;if(required)return required==='any'||required===group;if(LEGACY_REGIMENTAL_SUPPORTS.includes(key))return group==='infantry';return true;}`,`function regimentalBaselineCompatible(side,c,key=null){const group=regimentGroup(side,c);if(!key)return !!group;const structural=supportAllowedBattalionGroups(key,supports[key]);if(structural.groups.length)return regimentalSupportAllowed(key,supports[key],group);const required=supports[key]?.regimentGroup;if(required)return required==='any'||required===group;if(LEGACY_REGIMENTAL_SUPPORTS.includes(key))return group==='infantry';return false;}`,'regimental support compatibility'],
[`function syncDesignerSide(side){ensureDesignerState(side);state[side]=gridToCounts(state[side+'Grid'],Object.keys(battalions));}`,`function syncDesignerSide(side){ensureDesignerState(side);state[side]=gridToCounts(state[side+'Grid'],Object.keys(battalions));const regKey=side+'RegimentalSupports';state[regKey]=state[regKey].map((key,c)=>key&&filledInRegiment(state[side+'Grid'],c)>=3&&regimentalBaselineCompatible(side,c,key)?key:null);}`,'regimental state cleanup'],
[`ensureDesignerState('attacker');ensureDesignerState('defender');ensureTechState('attacker');ensureTechState('defender');ensureTankState();ensureAirState();ensureMioState();`,`syncDesignerSide('attacker');syncDesignerSide('defender');ensureTechState('attacker');ensureTechState('defender');ensureTankState();ensureAirState();ensureMioState();`,'initial regimental normalization'],
[`<p class=\"muted\">Bundled 1.19.2 support definitions are active. Structural regiment compatibility is enforced; research, DLC and Special Project requirements are informational only.</p>`,`<p class=\"muted\">Regimental support is filtered by the regiment's structural group. Retained game-file compatibility is used when available; the bundled compact pack uses a cross-checked fallback until its omitted compatibility metadata is recertified. Research, DLC and Special Project requirements remain informational only.</p>`,'picker evidence copy'],
[`if(designerPick?.kind==='regimental')state[side+'RegimentalSupports'][designerPick.c]=value;`,`if(designerPick?.kind==='regimental')state[side+'RegimentalSupports'][designerPick.c]=value&&regimentalBaselineCompatible(side,designerPick.c,value)?value:null;`,'picker assignment guard']
]);

console.log('Applied regimental-support source corrections.');
